# 代码缺陷扫描报告

- 扫描时间：2026-04-30 23:05:57
- 扫描模式：当前工作区目录
- 扫描路径：/Users/weizixiang/Desktop/coffer
- 用户名：unknown
- 后端 API：不可用，已使用默认扫描配置
- 扫描文件数：212
- 潜在缺陷数：4
- 确认缺陷数：4
- 过滤误报数：0

## 缺陷总览

| # | 风险等级 | 位置 | 风险代码 | 风险存在原因 |
|---|---------|------|---------|------------|
| 1 | 🔴 高风险 | lib/data/db/database.dart:479 | `if (fileSize < 4096)` | 旧明文 SQLite 数据库通常至少一个 page，可能大于等于 4096 字节；当前逻辑在无 `gwp.db.encrypted` marker 时只删除小文件，大明文库会被保留并按 SQLCipher 密钥打开，后续 `sqlite_master` 自检可能失败，导致升级用户数据库初始化失败、应用无法启动。 |
| 2 | 🔴 高风险 | lib/features/settings/presentation/dict_manage_page.dart:606 | `nominatim.openstreetmap.org` | 设置页字典管理中的坐标查询在 release 可达路径直接请求 Nominatim；项目外网约束仅允许 Frankfurter、东方财富、Yahoo Finance，并要求外部 IO 经 Provider 接口。该 UI 直连会把用户输入的地区/地名发送给非白名单第三方，且绕过统一超时/错误/隐私治理。 |
| 3 | 🔴 高风险 | lib/data/providers/asset/yahoo_finance_provider.dart:191 | `Decimal.tryParse(close.toString())` | Yahoo 历史 close 值来自外部响应；当前只校验可解析，不拒绝 0 或负数。`refreshHistory` 会将成功返回的点写入 `asset_price_history`，非正价格可污染历史估值数据，而不是作为 malformed response 失败。 |
| 4 | 🔴 高风险 | lib/data/providers/asset/eastmoney_provider.dart:197 | `Decimal.tryParse(cols[2])` | 东方财富 K 线 close 同样只校验可解析，不拒绝 0 或负数。异常上游数据会以成功序列进入历史价格写入路径，造成持久化价格/估值污染。 |

## 建议修复优先级

1. 先修复外部数据写入污染：Yahoo / Eastmoney 历史价统一要求 `price > 0`，并补充回归测试。
2. 移除或 debug-only 化 Nominatim 坐标查询，或改为本地字典/白名单 Provider。
3. 修复数据库明文检测逻辑：基于 SQLite 文件头识别明文库，并按既定产品策略删除或迁移，同时仅在加密库成功打开后写 marker。
