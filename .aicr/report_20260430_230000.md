# Coffer 代码缺陷扫描报告（第 7 次）

**扫描时间：** 2026-04-30 23:00
**扫描模式：** 全目录扫描
**扫描路径：** `/Users/weizixiang/Desktop/coffer/lib/`
**扫描文件数：** 182 个（排除 `.g.dart` / `.freezed.dart` 生成文件）
**扫描用户：** weizixiang
**工具版本：** map-code-review skill（双轮扫描 + 上下文复判）

---

## 统计摘要

| 指标 | 数值 |
|------|------|
| 第一轮发现潜在缺陷 | 28 个 |
| 第二轮确认缺陷 | 9 个 |
| 过滤误报 | 19 个 |
| 🔴 高风险（high confidence） | 7 个 |
| 🟡 中风险（medium confidence） | 2 个 |

---

## 确认缺陷详情

### DEFECT-1 — backup_restore.dart:182 — blob字段未做类型守卫
- **严重度：** 🔴 高风险
- **函数：** ImportBackupUseCase.call
- **缺陷代码：** `final blob = cipherMeta['blob'] as String;`
- **问题：** cipherMeta['blob'] 缺失时为 null，强转 TypeError 被 catch 包为 StorageError 而非 ValidationError，错误信息不明确，与同文件 saltRaw 的 is! String 模式不一致
- **修复：** `if (cipherMeta['blob'] is! String) return const Err(ValidationError('backup cipher blob missing or invalid'));`

### DEFECT-2 — refresh_asset_price.dart:146 — findById失败静默跳过
- **严重度：** 🔴 高风险
- **函数：** RefreshAssetPriceUseCase.refreshAll
- **缺陷代码：** `r.when(ok: (a) => targets.add(a), err: (_) {});`
- **问题：** 指定 assetIds 时 findById 失败的 err 分支为空块，既不刷新该资产也不计入 failed map，调用方完全无感知
- **修复：** `r.when(ok: (a) => targets.add(a), err: (e) => failed[id] = e.message);`

### DEFECT-3 — drift_asset_cost_history_repository.dart:27 — 查询失败返回空列表
- **严重度：** 🔴 高风险
- **函数：** listByAsset
- **缺陷代码：** `} catch (e, st) { print(...); return const []; }`
- **问题：** 查询失败与无历史记录不可区分，盈亏计算依赖此接口时以0成本计算，导致损益严重失真
- **修复：** 返回类型改为 `Future<Result<List<AssetCostHistoryPoint>, AppError>>`

### DEFECT-4 — drift_exchange_rate_repository.dart:56 — queryForDate失败写入错误历史汇率（不可逆）
- **严重度：** 🔴 高风险
- **函数：** queryForDate
- **缺陷代码：** `} catch (e, st) { print(...); return null; }`
- **问题：** 异常时返回 null 等同于"该日无记录"，调用方降级用 spot rate 写入历史，sourceKey 去重导致错误数据永久占位不可覆盖。代码注释已承认但未修复
- **修复：** 返回类型改为 `Future<Result<ExchangeRate?, AppError>>`，异常时返回 Err

### DEFECT-5 — account_list_page.dart:995 — softDelete结果静默丢弃
- **严重度：** 🔴 高风险
- **函数：** _confirmDelete
- **缺陷代码：** `await ref.read(accountRepositoryProvider).softDelete(a.id);`
- **问题：** 删除失败无任何用户反馈，与 account_detail_page.dart 同操作的完整处理形成不一致
- **修复：** 捕获返回值，err 分支 showSnackBar

### DEFECT-6 — account_detail_page.dart:240 — 可空 r 无 null 检查
- **严重度：** 🔴 高风险
- **函数：** _syncAllPrices
- **缺陷代码：** `if (r.isErr && r.errorOrNull!.message.contains('yahoo blocked')) {`
- **问题：** r 声明为可空 Result?，无 null 检查直接访问 .isErr，且 .errorOrNull! 使用 ! 操作符，代码脆弱
- **修复：** `if (r != null && r.isErr && (r.errorOrNull?.message.contains('yahoo blocked') ?? false))`

### DEFECT-7 — cached_fx_price_provider.dart:73 — 失败无负缓存导致请求风暴
- **严重度：** 🔴 高风险
- **函数：** getRate
- **缺陷代码：** finally { _inflight.remove(key); } (失败路径不写缓存)
- **问题：** getRate 失败后 _inflight 清除、_remoteCache 无记录，下次请求同一失败 pair 再次触发 HTTP，大量资产换算场景持续高频重试拖慢 UI
- **修复：** 失败路径写入短 TTL 负缓存（如30秒）

### DEFECT-8 — yahoo_finance_provider.dart:166 — 空数据集静默返回Ok
- **严重度：** 🟡 中风险
- **函数：** YahooFinanceProvider.fetchTimeSeries
- **缺陷代码：** `return Ok(AssetPriceSeries(symbol: ..., points: points, ...));`
- **问题：** 所有数据点解析失败时 points 为空仍返回 Ok，调用方无法区分成功有数据与成功无数据
- **修复：** `if (points.isEmpty) return const Err(NotFoundError('yahoo: no valid price points'));`

### DEFECT-9 — search_history_store.dart:105 — 非Map格式文件导致历史静默丢失
- **严重度：** 🟡 中风险
- **函数：** _load
- **缺陷代码：** `final j = jsonDecode(txt) as Map<String, dynamic>;`
- **问题：** 文件顶层非 Map 时 TypeError 被 catch 静默忽略，全部搜索历史丢失，Release 模式无任何日志
- **修复：** 强转前 `if (decoded is! Map<String, dynamic>)` 防御性检查

---

## 误报汇总（19 个过滤）

db_snapshot.dart:75（上游已校验）、topology_page.dart:427（Node.Id保证）、pin_store.dart:176（上游Map检查）、topology_page.dart:89（UUID固定36字符）、crypto_service.dart:52（调用方catch兜住）、account_list_page.dart:977（Flutter标准模式）、backup_page.dart:89（Flutter标准模式）、fixed_income_valuator.dart:259（负指数路径不可达）、dashboard_providers.dart:463/614（Ok<T>非空保证）、sync_providers.dart:79（Future正确传播）、dashboard_activity.dart:200（required String）、plan_transfer_route.dart:281（guard集合O(N)上界）、database.dart:117（Drift事务自动回滚）、channel_detail_page.dart:212（JSON序列化链路安全）

---

*报告由 Ducc map-code-review skill 自动生成*
