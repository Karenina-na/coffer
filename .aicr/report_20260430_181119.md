# 代码缺陷扫描报告

- 扫描时间：2026-04-30 18:11:19
- 扫描模式：当前工作区目录
- 扫描路径：/Users/weizixiang/Desktop/coffer
- 用户名：unknown
- 后端 API：不可用，已使用默认扫描配置
- 扫描文件数：208
- 潜在缺陷数：8
- 确认缺陷数：6
- 过滤误报数：2

## 缺陷总览

| # | 风险等级 | 位置 | 风险代码 | 风险存在原因 |
|---|---------|------|---------|------------|
| 1 | 🔴 高风险 | lib/features/settings/presentation/settings_page.dart:240 | `onTap: () => _seedMockData(context, ref)` | 设置页无 `kDebugMode` 或 flavor 守卫，release 可达 UI 会调用 `seedMockData` 向当前真实数据库写入演示账户、资产、卡、通道、事件和历史数据；确认 import 和调用路径均未被 debug-only 条件包裹，可能造成真实财务数据污染且无法一键撤销。 |
| 2 | 🔴 高风险 | lib/data/providers/asset/yahoo_finance_provider.dart:78 | `?? 'USD'` | Yahoo `meta.currency` 属于外部响应字段；当前价格存在但币种缺失/改名时会静默标记为 USD 并返回成功，下游估值按错误币种换算或持久化，未走 malformed/AppError 失败路径。 |
| 3 | 🔴 高风险 | lib/data/providers/asset/yahoo_finance_provider.dart:148 | `?? 'USD'` | Yahoo 历史序列缺少 `meta.currency` 时同样默认 USD，`refreshHistory` 会基于错误币种写入价格点和估值事件，导致历史资产价值污染而不是记录估值失败。 |
| 4 | 🟡 中风险 | lib/data/providers/fx/frankfurter_provider.dart:75 | `rates[k.toUpperCase()] = parsed` | Frankfurter 最新汇率解析接受 `0` 或负数；`RefreshWatchedRatesUseCase` 会把该值 upsert 到汇率表，后续价格换算可产生 0 或负资产/账户价值。 |
| 5 | 🟡 中风险 | lib/data/providers/fx/frankfurter_provider.dart:138 | `daily[s.toUpperCase()] = parsed` | Frankfurter 历史序列同样未校验正数，full refresh 可持久化非正历史汇率，污染 sparkline、历史换算和估值结果。 |
| 6 | 🟡 中风险 | lib/domain/valuation/strategies/fixed_income_valuator.dart:146 | `ext['compounding'] as String?` | `Asset.extInfo` 来自持久化 JSON/导入数据，若 `compounding` 存在但不是字符串，强制 cast 会抛 `TypeError`；`RefreshAssetPriceUseCase` 调用估值器时未包 try/catch，可能中断刷新流程而非返回 `AppError`。 |

## 过滤误报

- lib/domain/usecases/check_rate_alerts.dart:190：`'threshold': ?threshold?.toString()` 为 Dart 3 null-aware collection element，`flutter analyze` 未报语法错误。
- android/app/build.gradle.kts:39：release 使用 debug signing 已在 AGENTS.md 明确标注为当前验证包策略，不按本轮致命缺陷报告。

## 附加验证

- 已运行：`flutter analyze`
- 结果：未发现 error；存在 4 个既有 info 级 lint/deprecation 提示，未纳入致命缺陷。